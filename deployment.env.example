# Copy to the ignored `.deployment.env` file, restrict its permissions, and source it.
# Acceptance is intentionally not enabled in the repository.
TINKER_ACCEPT_OMNIVERSE_EULA=

ROS_DOMAIN_ID=25
RMW_IMPLEMENTATION=rmw_fastrtps_cpp

# Optional override for a restored, versioned asset cache.
# TINKER_ISAAC_ASSET_CACHE=/srv/tinker-sim-cache/isaac/6.0.1
