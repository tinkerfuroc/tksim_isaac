#!/usr/bin/env bash
set -euo pipefail

project_root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"
export PYTHONPATH="${project_root}/tools${PYTHONPATH:+:${PYTHONPATH}}"
exec python3 "${project_root}/tools/deploy.py" --root "${project_root}" "$@"
